DEV RUN

1) Root klasörde:
   npm install

2) Android (telefon) için tek komut:
   npm run dev:android

Not:
- AI_BASE_URL ve FLUTTER_DEVICE_ID değerlerini package.json içinden değiştirerek veya terminalde set ederek özelleştirebilirsin.
- Backend PORT=3000 varsayılır.
