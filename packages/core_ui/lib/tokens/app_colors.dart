import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF0ABFBC);
  static const primaryLight = Color(0xFF7FE1DF);
  static const primaryDark = Color(0xFF078F8D);

  // Semantic
  static const success = Color(0xFF5DCAA5);
  static const successDark = Color(0xFF0F6E56);
  static const warning = Color(0xFFFFB703);
  static const warningDark = Color(0xFF854F0B);
  static const error = Color(0xFFE24B4A);

  static const xp = warning;

  static const navy = Color(0xFF1B2A4A);

  // Neutral (dark theme)
  static const bg = Color(0xFF071A22);
  static const surface = Color(0xFF0B2430);
  static const surfaceElevated = Color(0xFF0E2B38);
  static const border = Color(0xFF123544);

  // Neutral (light theme)
  static const bgLight = Color(0xFFF0FCFC);
  static const surfaceLight = Color(0xFFF5F5F5);
  static const surfaceElevatedLight = Color(0xFFFFFFFF);
  static const borderLight = Color(0xFFE8E8E8);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const textMuted = Color(0xFF6B7C86);

  // Text (light)
  static const textPrimaryLight = Color(0xFF1B2A4A);
  static const textSecondaryLight = Color(0xFF555555);
  static const textMutedLight = Color(0xFF888888);
}
